<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>The Seer — Insight & Words</title>
  <meta name="description" content="The Seer — short, powerful illustrated posts about courage, truth and transformation. Follow for daily quotes and art." />
  <!-- Open Graph -->
  <meta property="og:title" content="The Seer — Insight & Words" />
  <meta property="og:description" content="Short, powerful illustrated posts about courage, truth and transformation." />
  <meta property="og:image" content="assets/og-image.jpg" />
  <meta name="theme-color" content="#0f0f0f" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@300;700&family=Inter:wght@300;600&display=swap" rel="stylesheet">
  <style>
    :root{
      --bg:#070606;
      --card:#0f0f0f;
      --accent:#c79a2a;
      --muted:#9a8e7a;
      --glass: rgba(255,255,255,0.03);
      --radius:16px;
      color-scheme: dark;
    }
    *{box-sizing:border-box}
    html,body{height:100%; margin:0; font-family:Inter,system-ui,-apple-system,Segoe UI,Roboto,"Helvetica Neue",Arial;}
    body{
      background: linear-gradient(180deg, #050505 0%, #0b0b0b 100%);
      color: #eee;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.45;
    }
    .container{max-width:1100px;margin:36px auto;padding:20px;}
    .hero{
      display:grid;
      grid-template-columns: 1fr;
      gap:20px;
      align-items:center;
    }
    @media(min-width:900px){ .hero{grid-template-columns: 1fr 420px;} }
    .left{
      padding:28px;
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius:var(--radius);
      box-shadow: 0 8px 30px rgba(0,0,0,0.6);
    }
    h1{font-family:Merriweather, serif; font-weight:700; font-size:28px; margin:0 0 12px; color:var(--accent);}
    p.lead{font-size:16px;color:var(--muted);margin:0 0 18px}
    .cta-row{display:flex;gap:12px;flex-wrap:wrap}
    .btn{
      background:transparent;
      border:1px solid rgba(255,255,255,0.06);
      color:#fff;
      padding:10px 14px;
      border-radius:10px;
      cursor:pointer;
      font-weight:600;
      text-decoration:none;
      transition: all .15s ease;
    }
    .btn.primary{background:linear-gradient(90deg,var(--accent), #e1b24a); color:#111; border:none;}
    .btn:hover{transform:translateY(-3px)}
    .card{
      background:linear-gradient(180deg, rgba(0,0,0,0.3), rgba(0,0,0,0.25));
      padding:18px;border-radius:12px;
    }
    .preview{
      border-radius:12px;
      overflow:hidden;
      position:relative;
      aspect-ratio:3/4;
      display:flex;
      align-items:flex-end;
      background: #111 url('assets/placeholder-bg.jpg') center/cover no-repeat;
    }
    .overlay-text{
      width:100%;
      padding:20px;
      backdrop-filter: blur(3px);
      background:linear-gradient(180deg, rgba(0,0,0,0.05), rgba(0,0,0,0.45));
    }
    .overlay-text h2{margin:0;font-family:Merriweather,serif;color:var(--accent);font-size:20px}
    .meta{display:flex; gap:10px; align-items:center; margin-top:10px}
    .badge{background:var(--glass); border-radius:10px;padding:6px 8px;font-size:13px;color:var(--muted)}
    footer{margin-top:28px; text-align:center; color:#999; font-size:13px}
    /* small niceties */
    .stat-row{display:flex;gap:12px;flex-wrap:wrap;margin-top:14px}
    .stat{background:rgba(255,255,255,0.02); padding:10px;border-radius:8px;min-width:120px;text-align:center}
    .stat b{display:block;font-size:18px;color:var(--accent)}
  </style>
</head>
<body>
  <div class="container">
    <div class="hero">
      <div class="left card">
        <h1>The Seer — Images & Words</h1>
        <p class="lead">Daily illustrated posts and short meditations — crafted to cut through the noise and reach the part of you that knows. Shareable art for seekers.</p>

        <div class="cta-row">
          <a class="btn primary" href="https://www.tiktok.com/@yourhandle" target="_blank">Visit TikTok</a>
          <a class="btn" href="#feed">Gallery</a>
        </div>

        <div class="stat-row">
          <div class="stat"><b>482K</b><span>Post views (60d)</span></div>
          <div class="stat"><b>5,831</b><span>Followers</span></div>
          <div class="stat"><b>48K</b><span>Likes</span></div>
        </div>

        <div style="margin-top:18px">
          <div class="card" id="feed">
            <div style="display:flex;gap:12px;align-items:center">
              <div style="flex:1">
                <p style="margin:0;color:var(--muted)">Latest post</p>
                <h3 style="margin:6px 0 0;font-family:Merriweather,serif;color:#fff">Freedom begins the moment you stop kneeling to false kings.</h3>
              </div>
              <div style="width:120px">
                <div class="preview" aria-hidden="true">
                  <div style="position:absolute;inset:0;background:linear-gradient(180deg,rgba(0,0,0,0.0),rgba(0,0,0,0.6));"></div>
                  <div class="overlay-text">
                    <h2>— THE SEER</h2>
                    <div class="meta"><span class="badge">Art • Quote</span><span class="badge">Dark & Golden</span></div>
                  </div>
                </div>
              </div>
            </div>
          </div> <!-- card -->
        </div>

      </div>

      <aside class="card">
        <div style="display:flex;gap:12px;align-items:center;margin-bottom:12px">
          <img src="assets/logo-circle.png" alt="The Seer logo" width="64" height="64" style="border-radius:8px;object-fit:cover">
          <div>
            <div style="font-weight:700">The Seer</div>
            <div style="color:var(--muted);font-size:13px">Daily posts • Insight • Imagery</div>
          </div>
        </div>

        <div style="margin-top:8px">
          <p style="margin:0 0 8px;color:var(--muted)">Follow the feed — or download assets for reposting.</p>
          <a class="btn primary" href="#" style="display:inline-block;width:100%;text-align:center">Follow on TikTok</a>
        </div>

        <div style="margin-top:16px">
          <div style="font-size:13px;color:var(--muted)">Subscribe for monthly image packs</div>
          <form style="margin-top:10px;display:flex;gap:8px">
            <input name="email" placeholder="Enter email" style="flex:1;padding:10px;border-radius:10px;border:1px solid rgba(255,255,255,0.04);background:transparent;color:#fff" />
            <button class="btn" type="submit">Join</button>
          </form>
        </div>
      </aside>

    </div>

    <footer>
      © <span id="year"></span> The Seer • Crafted with care. — <span style="color:var(--muted)">Profile: @yourhandle</span>
    </footer>
  </div>

  <script>
    // small nicety
    document.getElementById('year').textContent = new Date().getFullYear();
    // form demo: don't actually send unless you wire it up.
    document.querySelector('form')?.addEventListener('submit', (e)=>{e.preventDefault();alert('Thanks! Replace with real signup service (Mailchimp, ConvertKit).')});
  </script>
</body>
</html>
